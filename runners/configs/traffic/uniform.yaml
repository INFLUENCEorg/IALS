parameters:
    main:
        # Common hyperparameters
        env: 'traffic'
        simulator: 'partial'
        name: 'uniform_traffic'
        influence_model: 'uniform'
    influence:
        probs: [0.1, 0.1, 0.1, 0.1]