parameters:
    main:
        # Common hyperparameters
        env: 'traffic'
        simulator: 'local'
        name: 'traffic'
        influence_model: 'uniform'
        num_workers: 1
        policy: 'FNNPolicy'
        seq_len: 1
        framestack: false
        load_policy: false
        hidden_size: 512
        hidden_size_2: 256
        num_workers: 2
        epsilon: 0.1
        beta: 0.01
        render: False
        policy: 'FNNPolicy'
        load_policy: False

    influence:
        probs: [0.5, 0.5, 0.5, 0.5]
        seq_len: 1
        episode_length: 300